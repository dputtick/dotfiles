sudo apt update bash
# [ ] Install Firefox
# [ ] Use Mozilla sync to sync extensions
# [ ] Generate a new set of ssh keys: `ssh-keygen -t ed25519` + all default settings
# [ ] Add ssh public key to Github via the web interface
# [ ] sudo apt install git
# [ ] Follow instructions from README in dotfiles repo to install
# [ ] Run this script
# [ ] Install Sublime Text via the Sublime website
# [ ] Install Package Control and restart
# [ ] Copy the contents of the Sublime extensions User directory from dotfiles to the correct location
# [ ] Install rbenv, pyenv, and nvm via their respective install scripts
# [ ] Remap caps lock to ctrl in gnome-tweak
# [ ] Install Dropbox from website
# [ ] Install Rescuetime from website

sudo apt install tmux vim mosh htop curl gnome-tweak-tool fonts-inconsolata xclip
